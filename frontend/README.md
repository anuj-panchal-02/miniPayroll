# miniPayroll web

Next.js app for miniPayroll — public landing, Superadmin, company setup, and the payroll workspace.

See the repository root [README](../README.md) for product context, API setup, and how to run the stack.

```bash
npm run dev
```

The app is at [http://localhost:3000](http://localhost:3000). Existing users sign in at `/login`.
